diff --git a/app/src/main/assets/www/js/art.js b/app/src/main/assets/www/js/art.js
--- a/app/src/main/assets/www/js/art.js
+++ b/app/src/main/assets/www/js/art.js
@@
     // ============ 战场上武将塔（俯视角） ============
     drawTower(ctx, tower) {
         const { x, y, general, level } = tower;
         ctx.save();
         ctx.translate(x, y);
@@
         // 头像本体
         const r = 18 + level * 1.5;
-        ctx.translate(0, -8);
+        // 轻微呼吸/站姿起伏：仅视觉，不影响玩法与命中判定
+        const tt = performance.now() * 0.002;
+        const bob = Math.sin(tt + x * 0.01 + y * 0.01) * (0.6 + level * 0.12);
+        ctx.translate(0, -8 + bob);
@@
     _hero_guanyu(ctx, r, g) {
         // 关羽：红脸长须，绿袍金冠
         // 衣领
         ctx.fillStyle = "#1c5a2a";
@@
         ctx.bezierCurveTo(r * 0.3, r * 0.6, -r * 0.3, r * 0.6, -r * 0.5, r * 0.05);
         ctx.fill();
+        // 袍甲金边（提升服装辨识度）
+        ctx.strokeStyle = "rgba(247,215,116,.65)";
+        ctx.lineWidth = 2;
+        ctx.beginPath();
+        ctx.moveTo(-r * 0.92, r * 0.92);
+        ctx.lineTo(-r * 0.55, r * 0.25);
+        ctx.lineTo(0, r * 0.52);
+        ctx.lineTo(r * 0.55, r * 0.25);
+        ctx.lineTo(r * 0.92, r * 0.92);
+        ctx.stroke();
         // 红脸
         ctx.fillStyle = "#c43222";
         ctx.beginPath(); ctx.arc(0, -r * 0.05, r * 0.5, 0, Math.PI * 2); ctx.fill();
+        // 面部高光（更立体）
+        ctx.fillStyle = "rgba(255,230,200,.12)";
+        ctx.beginPath(); ctx.ellipse(-r * 0.18, -r * 0.12, r * 0.22, r * 0.16, -0.2, 0, Math.PI * 2); ctx.fill();
         // 卧蚕眉
         ctx.strokeStyle = "#1a0a04"; ctx.lineWidth = 2;
@@
         ctx.beginPath(); ctx.ellipse(-r * 0.18, -r * 0.05, 2, 1, 0, 0, Math.PI * 2); ctx.fill();
         ctx.beginPath(); ctx.ellipse(r * 0.18, -r * 0.05, 2, 1, 0, 0, Math.PI * 2); ctx.fill();
+        // 口（更威严）
+        ctx.strokeStyle = "rgba(30,10,6,.75)";
+        ctx.lineWidth = 1.2;
+        ctx.beginPath(); ctx.moveTo(-r * 0.06, r * 0.18); ctx.lineTo(r * 0.06, r * 0.18); ctx.stroke();
         // 金冠
         ctx.fillStyle = g.accent;
         ctx.fillRect(-r * 0.45, -r * 0.7, r * 0.9, r * 0.15);
         ctx.beginPath(); ctx.moveTo(-r * 0.45, -r * 0.55); ctx.lineTo(r * 0.45, -r * 0.55); ctx.lineTo(r * 0.3, -r * 0.4); ctx.lineTo(-r * 0.3, -r * 0.4); ctx.fill();
     },
@@
     _hero_zhangfei(ctx, r, g) {
         // 张飞：黑面虎须，绿袍燕领
         ctx.fillStyle = "#193b1f";
@@
         ctx.fillStyle = "#1a0a04";
         ctx.beginPath(); ctx.arc(-r * 0.18, -r * 0.05, r * 0.06, 0, Math.PI * 2); ctx.fill();
         ctx.beginPath(); ctx.arc(r * 0.18, -r * 0.05, r * 0.06, 0, Math.PI * 2); ctx.fill();
+        // 鼻梁 + 嘴（更凶）
+        ctx.strokeStyle = "rgba(20,10,6,.85)";
+        ctx.lineWidth = 1.6;
+        ctx.beginPath(); ctx.moveTo(0, -r * 0.02); ctx.lineTo(-r * 0.02, r * 0.10); ctx.stroke();
+        ctx.beginPath(); ctx.moveTo(-r * 0.10, r * 0.22); ctx.quadraticCurveTo(0, r * 0.30, r * 0.10, r * 0.22); ctx.stroke();
         // 虎须
         ctx.strokeStyle = "#1a0a04"; ctx.lineWidth = 2;
         for (let i = -2; i <= 2; i++) {
@@
         // 头巾
         ctx.fillStyle = g.accent;
         ctx.beginPath();
         ctx.moveTo(-r * 0.6, -r * 0.4); ctx.quadraticCurveTo(0, -r * 0.85, r * 0.6, -r * 0.4);
         ctx.lineTo(r * 0.6, -r * 0.55); ctx.quadraticCurveTo(0, -r * 0.95, -r * 0.6, -r * 0.55);
         ctx.fill();
+        // 护胸铆钉（增加“猛将”质感）
+        ctx.fillStyle = "rgba(247,215,116,.35)";
+        for (let i = -2; i <= 2; i++) {
+            ctx.beginPath(); ctx.arc(i * r * 0.16, r * 0.58, 1.5, 0, Math.PI * 2); ctx.fill();
+        }
     },
@@
     _hero_zhaoyun(ctx, r, g) {
         // 赵云：白袍银甲俊朗
         ctx.fillStyle = "#d2dde0";
@@
         // 银甲护肩
         ctx.fillStyle = "#9aa6b0";
         ctx.beginPath(); ctx.arc(-r * 0.7, r * 0.5, r * 0.3, 0, Math.PI * 2); ctx.fill();
         ctx.beginPath(); ctx.arc(r * 0.7, r * 0.5, r * 0.3, 0, Math.PI * 2); ctx.fill();
+        // 护心镜（更“银甲”）
+        ctx.fillStyle = "rgba(240,245,255,.35)";
+        ctx.beginPath(); ctx.ellipse(0, r * 0.42, r * 0.26, r * 0.18, 0, 0, Math.PI * 2); ctx.fill();
+        ctx.strokeStyle = "rgba(160,180,200,.6)";
+        ctx.lineWidth = 1.2;
+        ctx.beginPath(); ctx.ellipse(0, r * 0.42, r * 0.26, r * 0.18, 0, 0, Math.PI * 2); ctx.stroke();
         // 脸
         ctx.fillStyle = "#f3d6ad";
         ctx.beginPath(); ctx.arc(0, -r * 0.05, r * 0.5, 0, Math.PI * 2); ctx.fill();
@@
         // 嘴
         ctx.beginPath(); ctx.moveTo(-r * 0.06, r * 0.18); ctx.lineTo(r * 0.06, r * 0.18); ctx.strokeStyle = "#7a3a2a"; ctx.stroke();
+        // 红披风/战绶（让高速武将更“利落”）
+        ctx.fillStyle = "rgba(210,42,42,.85)";
+        ctx.beginPath();
+        ctx.moveTo(-r * 0.95, r * 0.95);
+        ctx.lineTo(-r * 0.35, r * 0.35);
+        ctx.lineTo(-r * 0.05, r * 0.55);
+        ctx.closePath();
+        ctx.fill();
         // 银盔
         ctx.fillStyle = "#c8cfd6";
@@
     _hero_zhugeliang(ctx, r, g) {
         // 诸葛亮：羽扇纶巾
         ctx.fillStyle = "#5a6f8a";
@@
         // 眼（智者半眯）
         ctx.strokeStyle = "#1a0a04"; ctx.lineWidth = 1.5;
         ctx.beginPath(); ctx.moveTo(-r * 0.28, -r * 0.05); ctx.lineTo(-r * 0.08, -r * 0.05); ctx.stroke();
         ctx.beginPath(); ctx.moveTo(r * 0.08, -r * 0.05); ctx.lineTo(r * 0.28, -r * 0.05); ctx.stroke();
+        // 微笑
+        ctx.strokeStyle = "rgba(90,40,30,.7)";
+        ctx.lineWidth = 1.2;
+        ctx.beginPath(); ctx.moveTo(-r * 0.06, r * 0.18); ctx.quadraticCurveTo(0, r * 0.23, r * 0.06, r * 0.18); ctx.stroke();
         // 纶巾
         ctx.fillStyle = "#e6e6e6";
@@
         ctx.fillStyle = "#222";
         ctx.beginPath(); ctx.arc(0, -r * 0.55, r * 0.06, 0, Math.PI * 2); ctx.fill();
+        // 羽扇（增强“谋略”标识）
+        ctx.strokeStyle = "rgba(255,255,255,.55)";
+        ctx.lineWidth = 1.6;
+        ctx.beginPath();
+        ctx.moveTo(r * 0.62, r * 0.10);
+        ctx.quadraticCurveTo(r * 0.92, r * 0.00, r * 0.72, -r * 0.22);
+        ctx.quadraticCurveTo(r * 0.56, -r * 0.06, r * 0.62, r * 0.10);
+        ctx.stroke();
     },
@@
     _hero_lvbu(ctx, r, g) {
         // 吕布：金甲飞将
         ctx.fillStyle = U.shade(g.color, -10);
@@
         // 金甲
         ctx.fillStyle = g.accent;
         ctx.fillRect(-r * 0.55, r * 0.25, r * 1.1, r * 0.18);
+        // 甲片分割线（更硬朗）
+        ctx.strokeStyle = "rgba(120,60,10,.55)";
+        ctx.lineWidth = 1.5;
+        ctx.beginPath();
+        ctx.moveTo(-r * 0.55, r * 0.34); ctx.lineTo(r * 0.55, r * 0.34);
+        ctx.moveTo(-r * 0.28, r * 0.25); ctx.lineTo(-r * 0.28, r * 0.43);
+        ctx.moveTo(r * 0.28, r * 0.25); ctx.lineTo(r * 0.28, r * 0.43);
+        ctx.stroke();
         // 脸
         ctx.fillStyle = "#f0d4ad";
         ctx.beginPath(); ctx.arc(0, -r * 0.05, r * 0.5, 0, Math.PI * 2); ctx.fill();
@@
         ctx.beginPath(); ctx.ellipse(-r * 0.18, -r * 0.02, 3, 2, 0, 0, Math.PI * 2); ctx.fill();
         ctx.beginPath(); ctx.ellipse(r * 0.18, -r * 0.02, 3, 2, 0, 0, Math.PI * 2); ctx.fill();
+        // 眼神红光（压迫感）
+        ctx.fillStyle = "rgba(220,40,30,.55)";
+        ctx.beginPath(); ctx.arc(-r * 0.18, -r * 0.02, 1.4, 0, Math.PI * 2); ctx.fill();
+        ctx.beginPath(); ctx.arc(r * 0.18, -r * 0.02, 1.4, 0, Math.PI * 2); ctx.fill();
@@
     _hero_zhouyu(ctx, r, g) {
         // 周瑜：青年俊雅吴将
         ctx.fillStyle = U.shade(g.color, -10);
@@
         // 笑唇
         ctx.strokeStyle = "#7a3a2a"; ctx.lineWidth = 1;
         ctx.beginPath(); ctx.moveTo(-r * 0.08, r * 0.18); ctx.quadraticCurveTo(0, r * 0.25, r * 0.08, r * 0.18); ctx.stroke();
+        // 发束/玉佩点（更“儒将”）
+        ctx.fillStyle = "rgba(255,255,255,.35)";
+        ctx.beginPath(); ctx.arc(-r * 0.22, -r * 0.32, 2, 0, Math.PI * 2); ctx.fill();
+        ctx.beginPath(); ctx.arc(r * 0.22, -r * 0.32, 2, 0, Math.PI * 2); ctx.fill();
         // 蓝盔
         ctx.fillStyle = U.shade(g.color, 30);
@@
     // ============ 大招特效 ============
     drawEffect(ctx, ef) {
         const t = ef.elapsed / ef.duration;
         ctx.save();
         switch (ef.kind) {
             case "flood":
                 // 沿路径绘制水流
                 ctx.strokeStyle = `rgba(80,170,255,${0.4 + 0.2 * Math.sin(ef.elapsed * 8)})`;
                 ctx.lineWidth = 38;
                 ctx.lineCap = "round"; ctx.lineJoin = "round";
@@
                 ctx.strokeStyle = `rgba(180,230,255,.7)`;
                 ctx.lineWidth = 12;
                 ctx.stroke();
                 // 浪花
                 for (let i = 0; i < 20; i++) {
                     const seed = (i * 53 + ef.elapsed * 100) % U.pathLength(ef.path);
                     const pt = U.pointOnPath(ef.path, seed);
                     ctx.fillStyle = "rgba(255,255,255,.8)";
                     ctx.beginPath(); ctx.arc(pt.x, pt.y, 3, 0, Math.PI * 2); ctx.fill();
                 }
+                // 水雾/泡沫粒子（少量，性能友好）
+                for (let i = 0; i < 22; i++) {
+                    const seed = (i * 71 + ef.elapsed * 140) % U.pathLength(ef.path);
+                    const pt = U.pointOnPath(ef.path, seed);
+                    const wob = Math.sin(ef.elapsed * 6 + i) * 10;
+                    ctx.fillStyle = "rgba(200,240,255,.28)";
+                    ctx.beginPath(); ctx.arc(pt.x + wob, pt.y - 6 + (i % 3), 2.2 + (i % 2), 0, Math.PI * 2); ctx.fill();
+                }
                 break;
             case "blaze":
                 // 火焰沿路径
                 for (let i = 0; i < ef.path.length - 1; i++) {
@@
                         ctx.fillStyle = grd;
                         ctx.beginPath(); ctx.arc(x, y, r, 0, Math.PI * 2); ctx.fill();
                     }
                 }
+                // 余烬上飘（低数量）
+                {
+                    const L = U.pathLength(ef.path);
+                    for (let i = 0; i < 26; i++) {
+                        const seed = (i * 97 + ef.elapsed * 220) % L;
+                        const pt = U.pointOnPath(ef.path, seed);
+                        const up = (ef.elapsed * 30 + i * 7) % 36;
+                        const sway = Math.sin(ef.elapsed * 3 + i) * 10;
+                        ctx.fillStyle = `rgba(255,180,80,${0.18 + 0.18 * Math.sin(ef.elapsed * 5 + i)})`;
+                        ctx.beginPath(); ctx.arc(pt.x + sway, pt.y - up, 1.8, 0, Math.PI * 2); ctx.fill();
+                    }
+                }
                 break;
             case "stun":
                 // 全场冲击波
                 const r = U.lerp(0, 700, Math.min(1, ef.elapsed / 0.6));
+                // 闪光（短促，不遮挡读图）
+                if (ef.elapsed < 0.18) {
+                    const a = 0.12 * (1 - ef.elapsed / 0.18);
+                    ctx.fillStyle = `rgba(255,220,140,${a})`;
+                    ctx.fillRect(0, 0, ctx.canvas.width, ctx.canvas.height);
+                }
                 ctx.strokeStyle = `rgba(255,200,80,${1 - Math.min(1, ef.elapsed / 0.6)})`;
                 ctx.lineWidth = 6;
                 ctx.beginPath(); ctx.arc(ef.x, ef.y, r, 0, Math.PI * 2); ctx.stroke();
                 ctx.strokeStyle = `rgba(255,255,255,${0.7 - Math.min(.7, ef.elapsed / 0.6)})`;
                 ctx.lineWidth = 3;
                 ctx.beginPath(); ctx.arc(ef.x, ef.y, r * 0.6, 0, Math.PI * 2); ctx.stroke();
+                // 星火
+                for (let i = 0; i < 10; i++) {
+                    const ang = i / 10 * Math.PI * 2 + ef.elapsed * 2;
+                    const rr = 40 + ef.elapsed * 120;
+                    const x = ef.x + Math.cos(ang) * rr;
+                    const y = ef.y + Math.sin(ang) * rr;
+                    ctx.fillStyle = "rgba(255,240,180,.45)";
+                    ctx.beginPath(); ctx.arc(x, y, 2, 0, Math.PI * 2); ctx.fill();
+                }
                 break;
             case "maze":
                 // 八阵图旋转
                 ctx.translate(ef.x, ef.y);
                 ctx.rotate(ef.elapsed * 1.2);
@@
                 ctx.strokeStyle = "rgba(122,216,255,.5)";
                 ctx.lineWidth = 2;
                 ctx.beginPath(); ctx.arc(0, 0, 60, 0, Math.PI * 2); ctx.stroke();
                 ctx.strokeStyle = "#fff"; ctx.lineWidth = 1;
                 ctx.beginPath(); ctx.arc(0, 0, 30, 0, Math.PI * 2); ctx.stroke();
+                // 八卦线（薄线，增强“阵法”感）
+                ctx.strokeStyle = "rgba(255,255,255,.18)";
+                ctx.lineWidth = 1;
+                for (let i = 0; i < 8; i++) {
+                    const a = i / 8 * Math.PI * 2;
+                    ctx.beginPath();
+                    ctx.moveTo(Math.cos(a) * 18, Math.sin(a) * 18);
+                    ctx.lineTo(Math.cos(a) * 68, Math.sin(a) * 68);
+                    ctx.stroke();
+                }
                 break;
             case "execute":
                 // 处决一击：金色十字 + 残影
                 ctx.translate(ef.x, ef.y);
                 ctx.rotate(ef.elapsed * 6);
@@
                 ctx.fillStyle = `rgba(255,255,255,${0.8 - 0.8 * t})`;
                 ctx.beginPath(); ctx.arc(0, 0, 10, 0, Math.PI * 2); ctx.fill();
+                // 斩击残影（更“重击”）
+                ctx.strokeStyle = `rgba(255,230,160,${0.45 * (1 - t)})`;
+                ctx.lineWidth = 3;
+                ctx.beginPath(); ctx.moveTo(-62, -18); ctx.lineTo(62, 18); ctx.stroke();
+                ctx.beginPath(); ctx.moveTo(-62, 18); ctx.lineTo(62, -18); ctx.stroke();
                 break;
             case "charge":
                 // 银影残像
                 for (let i = 0; i < ef.trail.length; i++) {
                     const tr = ef.trail[i];
                     const a = i / ef.trail.length;
                     ctx.fillStyle = `rgba(220,230,255,${a * 0.6})`;
                     ctx.beginPath(); ctx.arc(tr.x, tr.y, 12 * a, 0, Math.PI * 2); ctx.fill();
                 }
+                // 速度线
+                ctx.strokeStyle = "rgba(255,255,255,.25)";
+                ctx.lineWidth = 2;
+                for (let i = 0; i < ef.trail.length - 1; i += 2) {
+                    const a = ef.trail[i];
+                    const b = ef.trail[i + 1];
+                    ctx.beginPath(); ctx.moveTo(a.x, a.y); ctx.lineTo(b.x, b.y); ctx.stroke();
+                }
                 ctx.fillStyle = "#e8efff";
                 ctx.beginPath(); ctx.arc(ef.x, ef.y, 14, 0, Math.PI * 2); ctx.fill();
                 ctx.strokeStyle = "#fff"; ctx.lineWidth = 2;
                 ctx.beginPath(); ctx.moveTo(ef.x - 18, ef.y); ctx.lineTo(ef.x + 18, ef.y); ctx.stroke();
                 break;
             case "impact": {
