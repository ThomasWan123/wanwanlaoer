# 复制为 keystore.properties（勿提交 Git），用于 Release 正式签名
storeFile=release.keystore
storePassword=你的密钥库密码
keyAlias=release
keyPassword=你的密钥密码

# 生成密钥库示例（在项目根目录执行）：
# keytool -genkeypair -v -keystore release.keystore -alias release -keyalg RSA -keysize 2048 -validity 10000
